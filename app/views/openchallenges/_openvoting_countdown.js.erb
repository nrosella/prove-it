$(document).ready(function(){
  var finishTime = new Date(); 
  finishTime = new Date("<%= @challenge.challenge_end %>"); 
  $('#votingCountdown').countdown({until: finishTime, onExpiry: function(){
    $.ajax({
      url: "<%= open_voting_end_path %>", 
      dataType: "script", 
      type: "GET",
      data: {id: "<%= @challenge.id %>"}});
    },
  });
  
});