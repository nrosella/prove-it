//this is when voting timer goes to zero

$('#voting').remove();
$('#defaultCountdown').remove();
$('#votingCountdown').remove();
$('#in_progress').remove();
$('#status').html("<p>Status: closed</p>");
$('#closed').html("<h1><%= j @challenge.winner.name + ' wins!' %> </h1> <h4><%= @challenge.print_votes %> </h4>")
$('#evidence').html("<%= j render 'challenges/show_evidence' %>");

