$(document).ready(function(){
  var finishTime = new Date(); 
  finishTime = new Date("<%= @challenge.challenge_end + @challenge.voting_duration.minutes %>"); 
  $('#defaultCountdown').countdown({until: finishTime, onExpiry: function(){
    alert("voting done!");

    },
  });
});