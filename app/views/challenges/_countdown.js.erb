$(document).ready(function(){
  var finishTime = new Date(); 
  finishTime = new Date("<%= @challenge.challenge_end - 23.hours - 58.minutes - 20.seconds - 20.seconds - 1.minute %>"); 
 
  $('#defaultCountdown').countdown({until: finishTime, expiryUrl: '<%= challenge_path(@challenge) %>'});

})